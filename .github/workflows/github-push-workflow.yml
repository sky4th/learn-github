on:
 push:
   branches: # when someone pushes to main  or feature branch
      - 'feature/**'
   paths:
      - '**.js'
      - '**.py'
      - '**.tf'
   paths-ignore:
      - '**.txt'
   tags:
      - 'R1.**'
