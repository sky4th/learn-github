on:
 push:
   branches: # when someone pushes to main  or feature branch
      - 'feature/**'
   paths:
      - '**.js'
      - '**.py'
      - '**.tf'
      - '**.yml'
   tags:
      - 'R1.**'
jobs:
  githubObject:
    name: 'github object'
    runs-on: ubuntu-latest
    environment: 'dev'
    steps:
    - name: Check object
      run: |
          cat << OBJECT
          ${{ toJson(github) }}
          OBJECT
